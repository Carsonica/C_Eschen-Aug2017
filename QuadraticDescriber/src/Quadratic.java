
public class Quadratic {
	public static void quadrDescriber (double a, double b, double c) {
		//Check if the coefficients actually give a quadratic equation
		if(a == 0) {
			throw new IllegalArgumentException("The first coefficient cannot be 0");
		}
		//Check if the graph opens up or down
		if(a < 0) {
			boolean opensUp = false;
		} else {
			boolean opensup = true;
		}
		//Find the vertex by completing the square
		//Find the x-intercepts (copy-paste the quadForm program)
		//Find the y-intercept by looking at c (might not need program in this part
	}
}
